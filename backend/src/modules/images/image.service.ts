import fs from 'fs';
import config from '~/libs/config/config';


class ImageService {
  async upload(filePath: string, fileName: string): Promise<string> {

    const fileUrl = `https://`;
    return fileUrl;
  }
}

export { ImageService };
